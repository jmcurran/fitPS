% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap-inference.R
\name{bootstrapFit}
\alias{bootstrapFit}
\alias{bootstrapFit.psFit}
\title{Compute a bootstrap distribution for a fitted fitPS model}
\usage{
bootstrapFit(object, ...)

\method{bootstrapFit}{psFit}(
  object,
  B = 2000,
  level = 0.95,
  seed = NULL,
  silent = FALSE,
  parallel = TRUE,
  progressBar = FALSE,
  pbopts = list(type = "txt"),
  ...
)
}
\arguments{
\item{object}{A maximum likelihood \code{psFit} object.}

\item{...}{Additional arguments passed to methods.}

\item{B}{Number of bootstrap replicates.}

\item{level}{Confidence level used for percentile intervals.}

\item{seed}{Optional random seed for reproducible resampling.}

\item{silent}{Logical; suppress progress messages when \code{TRUE}.}

\item{parallel}{Logical; use parallel fitting when \code{TRUE}.}

\item{progressBar}{Logical; display a progress bar when \code{TRUE}.}

\item{pbopts}{Options passed to \code{pbapply::pboptions()} when progress
bars are requested.}
}
\value{
The input \code{psFit} object with a \code{psBootstrap} object
  attached as \code{bootstrap}.
}
\description{
Compute and attach a nonparametric bootstrap distribution to a maximum
likelihood \code{psFit} object. The returned fitted object contains a
\code{psBootstrap} object in \code{object$bootstrap}.
}
\details{
The bootstrap is an observation-level nonparametric bootstrap. Each
successful parameter replicate is transformed into the corresponding P or S
probabilities before probability summaries are calculated. Consequently,
the reported bootstrap mean probabilities are averages of transformed
bootstrap replicates, not probabilities evaluated at average bootstrap
parameter values.
}
\examples{
if (interactive()) {
data(Psurveys)
fit = fitZIDist(Psurveys$roux, nterms = 4)
fit = bootstrapFit(
  fit,
  B = 20,
  seed = 123,
  silent = TRUE,
  parallel = FALSE
)
bootstrapProbs(fit)

}
}
