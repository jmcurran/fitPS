# Stage 2.3.1 documentation spelling and grammar review

Stage 2.3.1 is a documentation-only cleanup. It edits roxygen/source documentation rather than generated `.Rd` files.

## Changes

- Fixed spelling mistakes in the `fitDist()` roxygen comments.
- Improved grammar and punctuation in the `fitDist()` and `fitZIDist()` help source.
- Standardised zeta shape wording around `shape > 1`.
- Updated README wording for the VGAM shifted-shape conversion.
- Left generated help files for `devtools::document()` to refresh during the stage workflow.
