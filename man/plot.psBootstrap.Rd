% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.psBootstrap.R
\name{plot.psBootstrap}
\alias{plot.psBootstrap}
\title{Plot bootstrap probability summaries}
\usage{
\method{plot}{psBootstrap}(
  x,
  n = NULL,
  showInterval = TRUE,
  ylim = NULL,
  xlab = "Probability term",
  ylab = "Bootstrap probability",
  main = "Bootstrap probability summaries",
  pch = 19,
  ...
)
}
\arguments{
\item{x}{An object of class \code{psBootstrap}.}

\item{n}{\code{NULL}, a single number of leading probabilities to plot, or a
vector of P or S indices to plot.}

\item{showInterval}{Logical; if \code{TRUE}, draw the stored percentile
confidence intervals.}

\item{ylim}{Optional numeric vector giving the y-axis limits.}

\item{xlab, ylab, main}{Optional plot labels.}

\item{pch}{Plotting symbol used for bootstrap means.}

\item{...}{Additional graphical arguments passed to \code{plot}.}
}
\value{
Invisibly returns the plotted bootstrap probability summary data
  frame.
}
\description{
Plot bootstrap mean probabilities and percentile confidence intervals for
the fitted P or S probabilities stored in a \code{psBootstrap} object.
}
\examples{
if (interactive()) {
data(Psurveys)
fit = fit(
  Psurveys$roux,
  model = zizModel(),
  method = "bootstrap",
  nterms = 4,
  B = 20,
  seed = 123,
  silent = TRUE,
  parallel = FALSE
)
plot(fit$bootstrap, n = 4)

}
}
