# Stage 9.5.2 setNames import repair

Stage 9.5.1 reached `R CMD check` successfully but produced a NOTE because the
generic numerical posterior implementation calls `setNames()` without importing
it from `stats`.

Stage 9.5.2 adds `@importFrom stats setNames` to the roxygen block for the generic
`fitNumericalPosteriorModel.psModel()` method. `NAMESPACE` remains generated by
roxygen and is not edited manually.

No numerical posterior calculations, public API behaviour, or engine dispatch
are changed by this repair.
