% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modelBayesContract.R
\name{modelFromUnconstrained}
\alias{modelFromUnconstrained}
\alias{modelFromUnconstrained.psModel}
\alias{modelFromUnconstrained.zetaModel}
\alias{modelFromUnconstrained.logarithmicModel}
\alias{modelFromUnconstrained.zizModel}
\title{Transform unconstrained coordinates to natural model parameters}
\usage{
modelFromUnconstrained(model, unconstrained, ...)

\method{modelFromUnconstrained}{psModel}(model, unconstrained, ...)

\method{modelFromUnconstrained}{zetaModel}(model, unconstrained, ...)

\method{modelFromUnconstrained}{logarithmicModel}(model, unconstrained, ...)

\method{modelFromUnconstrained}{zizModel}(model, unconstrained, ...)
}
\arguments{
\item{model}{A `psModel` object.}

\item{unconstrained}{Named numeric coordinates on the unconstrained scale.}

\item{...}{Additional model-specific transformation inputs.}
}
\value{
A named numeric vector of natural-scale model parameters, with names
  and order matching `modelParameterNames(model)`.
}
\description{
This is the inverse of [modelToUnconstrained()]. Generic MCMC calls it for
every proposed unconstrained state before evaluating the likelihood and
prior, both of which remain defined on the model's natural parameter scale.
}
\details{
The transformation must be one-to-one with [modelToUnconstrained()] over the
Bayesian parameter region, preserve the names and order returned by
[modelParameterNames()], and return valid natural-scale parameters whenever
its input is finite and valid for the chosen transformation. The default
`psModel` method is the identity transformation.

For example, if `modelToUnconstrained()` uses `log(theta)` for a positive
parameter, this method uses `exp(z)`. If it uses `qlogis(p)` for a
probability, this method uses `plogis(z)`.
}
\seealso{
[modelToUnconstrained()], [modelLogJacobian()]
}
