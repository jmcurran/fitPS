% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotUncertainty.R
\name{plotUncertainty}
\alias{plotUncertainty}
\alias{plotUncertainty.psFit}
\alias{plotUncertainty.psBootstrap}
\alias{plotUncertainty.psBayesianBootstrap}
\title{Plot parameter uncertainty for a fitPS inferential result}
\usage{
plotUncertainty(object, ...)

\method{plotUncertainty}{psFit}(
  object,
  level = c(0.8, 0.95),
  parameters = NULL,
  showPoints = FALSE,
  nGrid = 401,
  xlab = NULL,
  ylab = NULL,
  main = NULL,
  ...
)

\method{plotUncertainty}{psBootstrap}(
  object,
  level = c(0.8, 0.95),
  parameters = NULL,
  showPoints = FALSE,
  nGrid = 401,
  xlab = NULL,
  ylab = NULL,
  main = NULL,
  ...
)

\method{plotUncertainty}{psBayesianBootstrap}(
  object,
  level = c(0.8, 0.95),
  parameters = NULL,
  showPoints = FALSE,
  nGrid = 401,
  xlab = NULL,
  ylab = NULL,
  main = NULL,
  ...
)
}
\arguments{
\item{object}{A `psFit`, `psBootstrap`, or `psBayesianBootstrap` object.}

\item{...}{Additional graphical arguments passed to the initial plot call.}

\item{level}{One or more probability levels strictly between zero and one.}

\item{parameters}{Optional character vector naming one or two parameters to
display. By default all available parameters are used when there are at
most two.}

\item{showPoints}{Logical; for sample-based two-dimensional displays, show
the stored parameter replicates behind the contours.}

\item{nGrid}{Number of points used for one-dimensional likelihood/density
displays.}

\item{xlab, ylab, main}{Optional plot labels.}
}
\value{
Invisibly returns a list describing the plotted uncertainty
  representation, including its inferential interpretation and interval or
  contour coordinates.
}
\description{
Draw a common one- or two-parameter uncertainty display while preserving the
statistical interpretation of the fitted method. Maximum-likelihood fits use
likelihood-ratio confidence intervals or regions, ordinary bootstrap fits use
the stored bootstrap parameter distribution, parametric Bayesian fits use the
stored posterior representation, and Rubin Bayesian Bootstrap fits use the
stored weighted-fit parameter distribution.
}
\details{
The visual grammar is deliberately consistent across inferential methods,
but the regions are not interchangeable. MLE regions are likelihood-ratio
confidence regions. Ordinary bootstrap regions are smoothed confidence
regions based on the bootstrap estimator distribution. Parametric Bayesian
regions are posterior credible regions. Rubin Bayesian Bootstrap regions
describe the distribution of weighted maximum-likelihood fits induced by
Bayesian Bootstrap weights.

For two-dimensional numerical Bayesian fits, fitPS reuses the stored
posterior summary grid. Probability-content contours are obtained by ordering the stored grid points
by posterior density and cumulatively summing their stored quadrature mass;
plotting therefore does not rerun adaptive cubature. For MCMC and bootstrap
representations, contours are obtained from the stored parameter draws using
kernel density estimation. Importance-sampling posteriors retain their sample
weights for weighted KDE contours, while Laplace fits use Gaussian contours
implied by the stored local covariance approximation.
}
\examples{
if (interactive()) {
  data(Psurveys)
  roux = Psurveys$roux

  mleFit = fit(roux, model = zizModel())
  plotUncertainty(mleFit, level = c(0.80, 0.95))

  bayesFit = fit(
    roux,
    model = zizModel(),
    method = "bayes",
    bayesOptions = list(posteriorMethod = "numerical")
  )
  plotUncertainty(bayesFit, level = c(0.80, 0.95))
}

}
