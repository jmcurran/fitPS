% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psModel.R
\name{psModel}
\alias{psModel}
\title{Construct a public fitPS model descriptor}
\usage{
psModel(
  model,
  parameterNames,
  subclass,
  supportedEngines = character(),
  mleStart = NULL,
  mleLower = NULL,
  mleUpper = NULL
)
}
\arguments{
\item{model}{Non-empty model identifier stored in fitted objects.}

\item{parameterNames}{Unique natural-scale parameter names.}

\item{subclass}{Concrete S3 subclass for external method dispatch.}

\item{supportedEngines}{Character vector of supported Bayesian posterior
engines. MLE-only models may use `character()`.}

\item{mleStart}{Optional named numeric starting values for generic MLE.}

\item{mleLower}{Optional named numeric lower bounds. Missing bounds default to
`-Inf`.}

\item{mleUpper}{Optional named numeric upper bounds. Missing bounds default to
`Inf`.}
}
\value{
An object inheriting from `psModel` and `subclass`.
}
\description{
Creates a model descriptor that can be defined outside fitPS and passed to
[fit()]. External model classes supply model-specific mathematics through the
exported S3 generics documented here. For simple maximum-likelihood models,
fitPS can own the numerical optimisation when starting values and parameter
bounds are supplied.
}
\examples{
model = psModel(
  model = "example",
  parameterNames = "theta",
  subclass = "exampleModel",
  mleStart = c(theta = 1),
  mleLower = c(theta = 0)
)
}
