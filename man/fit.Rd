% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit}
\alias{fit}
\title{Fit a fitPS model}
\usage{
fit(
  x,
  model,
  nterms = 10,
  method = c("mle", "bayes", "integrate", "numerical", "mcmc", "laplace", "importance"),
  prior,
  bayesOptions = NULL,
  ...
)
}
\arguments{
\item{x}{An object of class `psData`.}

\item{model}{A model descriptor inheriting from `psModel`, such as an object
returned by `zetaModel()`, `zizModel()`, or `logarithmicModel()`.}

\item{nterms}{Number of fitted P/S probability terms to retain.}

\item{method}{Fitting method. Built-in models currently preserve the methods
accepted by their established fitting functions.}

\item{prior}{Optional prior used for Bayesian fitting.}

\item{bayesOptions}{Optional Bayesian fitting controls.}

\item{...}{Additional controls passed to the model-specific fitting path.}
}
\value{
An object of class `psFit` retaining the originating model descriptor
  in `modelObject` and the established character identifier in `model`.
}
\description{
Fits a `psModel` object to forensic P- or S-survey data. This is the common
model-oriented fitting entry point introduced for the fitPS public model
API. Stage 8.2 supports the built-in zeta, zero-inflated zeta, and
logarithmic model descriptors. The public third-party model contract is
introduced separately so it can be validated against an external model.
}
\examples{
data(Psurveys)
zetaFit = fit(Psurveys$roux, model = zetaModel())
logFit = fit(Psurveys$roux, model = logarithmicModel())
}
